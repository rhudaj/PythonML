Whenever you need to use the Python environment with the installed packages, activate the virtual environment:

    source ~/myenv/bin/activate

To deactivate the virtual environment, simply run:

    deactivate